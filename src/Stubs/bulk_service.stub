<?php

declare(strict_types=1);

namespace {{ namespace }};

use {{ modelNamespace }};
{{ dataImport }}use Illuminate\Support\Collection;
use Mrmarchone\LaravelAutoCrud\Services\BulkService;

class {{ class }} extends BulkService
{
    protected string $modelClass = {{ model }}::class;

    {{ uniqueKeyProperty }}
    protected function transformToDataObjects(array $dataArray): Collection
    {
        {{ transformationLogic }}
    }
}
